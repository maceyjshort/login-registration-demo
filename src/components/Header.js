export default ({ title }) =>
  `<h1 class="my-4 text-center text-3xl font-semibold">${title}</h1>`;

// Header remains us that it is a compoent
// title knows it will recieve something to be passed on something
// title is a prop being passed to a child
